pipeline {
    agent any

    stages {
        stage('Clonar o repositorio') {
            steps {
                git 'https://github.com/Andersonalmeida99/TESTES-MOBILE-EBAC-SHOP.git'
        }
    }
        stage('Instalar dependencias') {
            steps {
                bat'npm install'
            }
        }
        stage('Executar Testes') {
            steps {
                bat 'npm run cy:run'
            }
        }
    }
}
